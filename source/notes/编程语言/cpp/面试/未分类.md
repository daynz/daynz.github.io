### 智能指针



### RAII

RAII是C++的资源管理机制，全称Resource Acquisition Is Initialization，资源获取即初始化。

核心是把资源（内存、文件句柄等）和对象生命周期绑定：对象创建时获取资源，析构时自动释放。

无需手动调用释放函数，能避免资源泄漏。

比如std::unique_ptr就是典型应用，对象出作用域析构，自动释放指向的内存，让资源管理更安全、简洁。

